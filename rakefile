require __dir__ + '/scripts/validate'

task :validate do
  base_uri = 'http://kasp853b.keaweb.dk/jepti-webshop'

  PatternValidator.validate('./assets', '*.css', print_errors: true, validates_css: true)
  RemoteValidator.validate(base_uri, '.', '*.php', print_errors: true)
end